import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.wardrobeplanner.app',
  appName: 'Wardrobe Planner',
  webDir: 'dist/garde-robe-budget-app/browser'
};

export default config;
